for _ in range(int(input())):
    s = list(input())
    t = [s[0]]
    for i in range(1,len(s)):
        if s[i - 1] == s[i]:
            continue
        else:
            t.append(s[i])
    print(len(s) - len(t))
